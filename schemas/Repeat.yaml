type: object
properties:
  amount:
    description: the amount of time
    type: integer
    format: int64
  unit:
    type: string
    enum:
      - day
      - week
      - month
      - year
  time:
    type: string
    format: time
    description: for day
